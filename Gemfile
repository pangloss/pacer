# A sample Gemfile
source "http://rubygems.org"

gemspec

# You must clone git://github.com/pangloss/pacer-orient.git into
# the path specified below and then in that folder, run `rake build`
# to build the necessary jar.
gem 'pacer-orient', '1.0.2', :path => '../pacer-orient'
